# Faculty Appraisal System - Environment Variables

# Backend API Base URL
# For local development, use: http://localhost:5000
# For production, use your production API URL: https://api.yourdomain.com
VITE_BASE_URL=http://localhost:5000

# Note: All environment variables in Vite must be prefixed with VITE_
# These variables will be embedded in the client bundle at build time
# DO NOT include sensitive information like API keys or secrets

# Example production configuration:
# VITE_BASE_URL=https://api.production-domain.com